#!/bin/bash
set -euo pipefail

echo "Converting json results to csv ..." >> $LOG_FILE
pushd $OUTPUT_FOLDER
# Assumes only membrane.v column
echo "membrane.v" > output.csv
cat output.json | jq -r '.membrane.v[]' >> membrane-potential.csv

popd
